# Environment variables template for Feedback Collection Backend

# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
# For local Prisma development database (replace with your actual connection string)
DATABASE_URL="prisma+postgres://localhost:51213/?api_key=your_api_key_here"

# Alternative: Traditional PostgreSQL connection string
# DATABASE_URL="postgresql://username:password@localhost:5432/feedback_collection?schema=public"

# Alternative: SQLite for development (uncomment if using SQLite)
# DATABASE_URL="file:./dev.db"

# JWT Configuration (if implementing authentication)
# JWT_SECRET=your_jwt_secret_here
# JWT_EXPIRES_IN=7d

# CORS Configuration (optional)
# CORS_ORIGIN=http://localhost:3000

# Email Configuration (if implementing email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# API Keys (if integrating with external services)
# EXTERNAL_API_KEY=your_api_key_here

# Redis Configuration (if implementing caching)
# REDIS_URL=redis://localhost:6379

# Logging Level
# LOG_LEVEL=info
